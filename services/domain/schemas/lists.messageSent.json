{
  "type": "object",
  "required": ["listId", "messageId", "text", "sentBy"],
  "properties": {
    "listId": { "type": "string", "minLength": 1, "maxLength": 100 },
    "messageId": { "type": "string", "minLength": 1, "maxLength": 100 },
    "text": { "type": "string", "minLength": 1, "maxLength": 500 },
    "sentBy": { "type": "string", "minLength": 1, "maxLength": 100 }
  }
}
